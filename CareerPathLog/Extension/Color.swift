import SwiftUI

extension Color {
    static let theme = ColorTheme()
}

struct ColorTheme {
    let mainColor = Color("Yellow")
    let deniedColor = Color("Orange")
    let whiteColor = Color.white
    let blackColor = Color.black
    let link = Color("BlueLink")
    let blueText = Color("BlueText")
}
